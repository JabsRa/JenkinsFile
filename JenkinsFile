pipeline {
    agent any
    stages {
        stage('Katalon Test') {
            steps {
                dir('path/to/katalon/project') {
                    sh 'C:\\Users\\jabal\\git\\Katalon-Test -noSplash -runMode=console -projectPath="." -retry=0 -testSuitePath="Test Suites/TestSuite1"'
                }
            }
        }
    }
}
